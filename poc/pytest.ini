[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    integration: real FFmpeg + real OpenAI API calls (network, costs money) — opt-in only, run with `pytest -m integration`
addopts = -m "not integration"
