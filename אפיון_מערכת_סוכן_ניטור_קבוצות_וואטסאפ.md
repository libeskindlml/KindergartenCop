# אפיון מוצר – "GroupGuard": סוכן ניטור לקבוצות וואטסאפ (SaaS)

**גרסה:** 1.0 | **תאריך:** 05.09.2026 | **סטטוס:** טיוטה לאישור פיתוח

---

## 1. תקציר מנהלים

GroupGuard היא פלטפורמת SaaS רב-דיירית (Multi-Tenant) שבמרכזה "סוכן" – מספר וואטסאפ וירטואלי שמצורף כחבר לקבוצת וואטסאפ של הלקוח. הסוכן קורא את כל התעבורה בקבוצה (טקסט, תמונות, סטיקרים, ובהמשך גם וידאו/קול), מבין את ההקשר שלה באמצעות מודלי שפה וראייה, ומספק שני ערכים עיקריים:

1. **אכיפה בזמן אמת** – השוואת כל הודעה מול סט חוקי הקבוצה שהגדיר הלקוח, ושליחת התראה בוואטסאפ לאנשי הקשר שהוגדרו כשמתבצעת הפרה.
2. **דו"ח יומי במייל** – סטטיסטיקות פעילות (כמות הודעות, משתתפים פעילים, נושאים חמים, הפרות שנרשמו) שנשלח דרך Gmail API.

כל לקוח (Tenant) יכול לנהל מספר קבוצות, ולכל קבוצה סט חוקים, כתובות מייל ומספרי טלפון לדיווח משלה.

### מטרות עסקיות
- מכירת מנויים חודשיים/שנתיים לפי חבילות (לפי מספר קבוצות, נפח הודעות, פיצ'רים).
- זמן Onboarding של לקוח חדש: עד 10 דקות ללא מעורבות אנושית.
- אפס הזדקקות של הלקוח לידע טכני.

### מה המסמך הזה לא מכסה
- עיצוב UI/UX מפורט (מסכי Figma יסופקו בנפרד).
- תמחור סופי של החבילות.

---

## 2. משתמשים ופרסונות

| פרסונה | תיאור | צרכים עיקריים |
|---|---|---|
| **מנהל קהילה** (לקוח) | מנהל/ת קבוצת וואטסאפ עסקית, קהילתית או שכונתית | הגדרת חוקים בשפה פשוטה, קבלת התראות, דו"ח יומי |
| **מקבל התראות** | איש קשר שהוגדר ע"י הלקוח (יכול להיות שהוא לא משתמש רשום במערכת) | לקבל הודעת וואטסאפ ברורה ומיידית עם ההקשר של ההפרה |
| **אדמין מערכת** (Anthropic-side / הספק) | צוות התפעול של הפלטפורמה | ניהול Tenants, ניטור בריאות החיבורים, טיפול בתקלות, ניהול מספרים וירטואליים |
| **חבר קבוצה** | אינו משתמש במערכת, אך המערכת מעבדת את המידע שלו | פרטיות, שקיפות (הודעת גילוי בקבוצה) |

---

## 3. היקף המערכת ו-MVP

### 3.0 שלב POC (הוכחת היתכנות) – לפני ה-MVP
מטרת השלב: לוודא שהרעיון עובד מקצה לקצה על קבוצה אמיתית אחת, לפני השקעה במוצר SaaS מלא.
- קבוצת וואטסאפ אחת, Tenant אחד (ללא הרשמה עצמית – חשבון שנוצר ידנית).
- מספר וירטואלי אחד מחובר לקבוצה, קליטת טקסט, תמונות וסטיקרים.
- הבנת מדיה וסט חוקים קטן (2–3 דטרמיניסטיים + 2–3 סמנטיים) שמוגדרים בקובץ קונפיגורציה, לא בפאנל.
- התראות וואטסאפ לרשימת אנשי קשר קבועה.
- דו"ח יומי אחד במייל דרך Gmail API.
- פאנל HTML מינימלי לצפייה בהודעות ובהפרות בלבד.
- **ללא חיבור לספק תשלומים** וללא ניהול מנויים/חבילות – יתווספו רק אחרי אישור ה-POC.
- קריטריון הצלחה: 7 ימים רצופים של פעילות ללא ניתוקים, זיהוי נכון של ≥ 80% מההפרות בבדיקה ידנית, ודו"ח יומי שמגיע בזמן.

### 3.1 בתוך ה-MVP (שלב 1)
- הרשמה, התחברות, ניהול מנוי בסיסי.
- חיבור מספר וירטואלי לקבוצה (הלקוח מוסיף את המספר לקבוצה).
- קליטת הודעות טקסט, תמונות וסטיקרים בזמן אמת.
- הבנת תמונות וסטיקרים (תיאור טקסטואלי + סיווג).
- מנוע חוקים היברידי: חוקים דטרמיניסטיים (מילות מפתח, שעות, קישורים, ספאם) + חוקים סמנטיים (LLM).
- התראות וואטסאפ בזמן אמת לרשימת אנשי קשר לכל קבוצה.
- דו"ח יומי במייל דרך Gmail API.
- פאנל ניהול HTML ללקוח ולאדמין.

### 3.2 מחוץ ל-MVP (שלבים 2–3)
- **גרסה 2:** תמלול הודעות קוליות (אודיו) והכללתן במנוע החוקים ובדו"ח.
- **גרסה 2:** משוב ישירות מתוך ההתראה – מקבל ההתראה יכול להשיב בוואטסאפ (למשל "1 = מוצדקת", "2 = קריאת שווא") והמשוב נרשם על ההפרה ומשמש את המנוע כדוגמה ללמידה ולדיוק עתידי, כדי לצמצם התראות שווא. בנוסף, אפשרות להעלות דוגמה של הפרה שהמערכת פספסה (מהפאנל או ע"י העברת ההודעה למספר הסוכן), שתתווסף כדוגמה חיובית לחוק הרלוונטי.
- **גרסה 3:** תמלול והבנת וידאו.
- דו"חות שבועיים/חודשיים ו-Dashboard אנליטי.

### 3.3 מחוץ לסקופ הפרויקט
- **פעולות אכיפה אוטומטיות** (מחיקת הודעה / הסרת חבר / השתקה) – אינן חלק מהפרויקט. המערכת מתריעה בלבד, והאכיפה נשארת בידי מנהלי הקבוצה. ייתכן שיישקל בשלב מאוחר הרבה יותר, בכפוף ליכולות ספק הוואטסאפ ולהחלטה מוצרית נפרדת.
- **חיבור לפלטפורמות נוספות** (טלגרם / Slack / Discord) – הפרויקט מתמקד בוואטסאפ בלבד.
- **API ציבורי ו-Webhooks ללקוחות** – לא בתכולה.

---

## 4. ארכיטקטורה כללית

```
┌──────────────┐    Webhook / WS    ┌──────────────────┐
│  WhatsApp    │ ─────────────────► │  WA Connector    │
│  (מספר וירט')│ ◄───────────────── │  Service         │
└──────────────┘   send alerts      └────────┬─────────┘
                                             │ raw events
                                             ▼
                                   ┌──────────────────┐
                                   │  Message Queue   │  (Redis Streams / RabbitMQ)
                                   └────────┬─────────┘
                                            │
              ┌──────────────┬──────────────┼───────────────┐
              ▼              ▼              ▼               ▼
      ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐
      │ Ingestion  │  │  Media     │  │  Rules     │  │ Analytics  │
      │  Worker    │  │ Understand.│  │  Engine    │  │  Worker    │
      └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘
            │               │               │               │
            ▼               ▼               ▼               ▼
      ┌──────────────────────────────────────────────────────────┐
      │        PostgreSQL (multi-tenant)  +  Object Storage      │
      └──────────────────────────────────────────────────────────┘
                              ▲
        ┌─────────────────────┼──────────────────────┐
        ▼                     ▼                      ▼
┌──────────────┐    ┌──────────────────┐   ┌──────────────────┐
│ Admin/Client │    │ Notification     │   │ Scheduler        │
│ Web (HTML)   │    │ Service (WA+Mail)│   │ (daily reports)  │
└──────────────┘    └──────────────────┘   └──────────────────┘
```

### 4.1 עקרונות ארכיטקטוניים
- **Provider Abstraction** – כל תקשורת עם וואטסאפ עוברת דרך ממשק `WhatsAppProvider` אחיד, כך שניתן להחליף ספק מבלי לגעת בלוגיקה העסקית (ראה סעיף 10.2 – זהו הסיכון הטכנולוגי המרכזי של הפרויקט).
- **Event-Driven** – כל הודעה נכנסת היא אירוע בתור; עיבוד אסינכרוני עם Retry ו-Dead-Letter Queue.
- **Tenant Isolation** – כל שורה בבסיס הנתונים נושאת `tenant_id`; אכיפה ברמת ORM (query scope) וברמת DB (Row-Level Security ב-PostgreSQL).
- **Idempotency** – כל אירוע נושא `provider_message_id` ייחודי; עיבוד כפול לא יוצר רשומות כפולות או התראות כפולות.
- **Fail-Safe** – כשל במודל ה-AI לא מפיל את הקליטה; ההודעה נשמרת ומסומנת לעיבוד חוזר.

---

## 5. דרישות פונקציונליות

### 5.1 ניהול Tenants ומנויים
| מזהה | דרישה |
|---|---|
| F-1.1 | הרשמה עם מייל + סיסמה, אימות מייל, ואופציונלית Google Sign-In. |
| F-1.2 | לכל Tenant: שם, מדינה, אזור זמן (ברירת מחדל Asia/Jerusalem), שפת ממשק (עברית/אנגלית, RTL מלא). |
| F-1.3 | חבילות מנוי: Free-Trial (14 יום, קבוצה אחת), Basic, Pro, Enterprise. הגבלות לפי: מס' קבוצות, מס' הודעות/חודש, מס' אנשי קשר להתראות, שימוש בחוקים סמנטיים. |
| F-1.4 | חיוב דרך ספק סליקה ישראלי (בחירת הספק תיעשה בשלב ה-SaaS; לא רלוונטי לגרסאות הראשוניות). המערכת לא מאחסנת פרטי אשראי. |
| F-1.5 | תפקידים בתוך Tenant: Owner, Admin, Viewer. |
| F-1.6 | השעיית Tenant (אי-תשלום / הפרת תנאים) מפסיקה עיבוד אך שומרת נתונים למשך 30 יום. |

### 5.2 חיבור קבוצה (Onboarding)
| מזהה | דרישה |
|---|---|
| F-2.1 | הלקוח לוחץ "הוסף קבוצה" → המערכת מקצה לו מספר וירטואלי מהמאגר (Pool) או מספר ייעודי (בחבילות גבוהות). |
| F-2.2 | מוצגות הוראות: "הוסף את המספר +XXX לקבוצה". המערכת מזהה אוטומטית את אירוע ההצטרפות ומציגה את שם הקבוצה לאישור. |
| F-2.3 | לאחר האישור, הסוכן שולח (אופציונלי, מופעל כברירת מחדל) הודעת גילוי נאות לקבוצה: "קבוצה זו מנוטרת ע"י GroupGuard לצורך אכיפת חוקי הקבוצה". נדרש מטעמי חוק הגנת הפרטיות. |
| F-2.4 | מספר וירטואלי אחד יכול לשרת מספר קבוצות של אותו Tenant בלבד (לא בין Tenants שונים – מטעמי פרטיות). |
| F-2.5 | ניתוק קבוצה: הסרת המספר מהקבוצה או לחיצה על "נתק" בפאנל. הנתונים ההיסטוריים נשמרים לפי מדיניות השמירה. |
| F-2.6 | סטטוס חיבור מוצג בזמן אמת: מחובר / מנותק / ממתין להוספה / חסום ע"י וואטסאפ. |

### 5.3 קליטת הודעות (Ingestion)
| מזהה | דרישה |
|---|---|
| F-3.1 | קליטה של: טקסט, תמונה, סטיקר (סטטי ומונפש), וידאו (מטא-דאטה בלבד ב-MVP), קול (מטא-דאטה בלבד ב-MVP), מסמך, קישור, מיקום, איש קשר, סקר. |
| F-3.2 | קליטת אירועי קבוצה: הצטרפות, עזיבה, הסרה, שינוי שם/תיאור, שינוי מנהלים. |
| F-3.3 | קליטת עריכות ומחיקות של הודעות (סימון הרשומה כ-`edited`/`deleted`, שמירת הגרסה המקורית). |
| F-3.4 | קליטת תגובות (Reactions) ותשובות (Reply-to) לצורך הבנת הקשר. |
| F-3.5 | מדיה נשמרת ב-Object Storage מוצפן עם URL חתום זמני; ברירת מחדל: מחיקה אחרי 30 יום (ניתן לקביעה ברמת Tenant). |
| F-3.6 | זמן מקצה לקצה מקבלת ההודעה ועד שמירתה ב-DB: < 2 שניות (P95). |

### 5.4 הבנת מדיה (Media Understanding)
| מזהה | דרישה |
|---|---|
| F-4.1 | **תמונות** – מודל Vision מייצר: תיאור טקסטואלי (עד 100 מילים), טקסט שמופיע בתמונה (OCR), דגלי תוכן (עירום, אלימות, מסמך אישי כמו ת"ז/כרטיס אשראי, צילום מסך של שיחה פרטית, פרסומת). |
| F-4.2 | **סטיקרים** – מזוהים כטיפוס נפרד. המודל מייצר: תיאור, רגש/כוונה (הומור, לעג, תמיכה, איום, מיני), ותוצאה בוליאנית "האם הסטיקר פוגעני בהקשר הנוכחי". |
| F-4.3 | **הקשר** – ניתוח מדיה תמיד מקבל את 5 ההודעות הקודמות בשיחה, כדי להבין למשל שסטיקר של "פרצוף צוחק" נשלח כתגובה לסיפור על תאונה. |
| F-4.4 | **Cache** – סטיקרים מזוהים לפי hash; סטיקר שכבר נותח לא ישלח שוב למודל (חיסכון בעלויות). ההקשר מנותח מחדש, התיאור לא. |
| F-4.5 | תוצאות ההבנה נשמרות כשדה `ai_analysis` (JSONB) על רשומת ההודעה ומשמשות את מנוע החוקים ואת הדו"ח. |

### 5.5 מנוע חוקים (Rules Engine)
מנוע החוקים הוא ליבת המוצר. הוא היברידי: **חוקים דטרמיניסטיים** (מהירים, זולים, ללא AI) ו**חוקים סמנטיים** (LLM).

#### 5.5.1 חוקים דטרמיניסטיים (מוגדרים בטופס, ניתנים לעריכה ושינוי על ידי המשתמש)
המשתמש בוחר נקודת פתיחה מאחד משלושה סטים מוכנים לפי רמת הקפדה, ויכול לאחר מכן להוסיף, להסיר ולערוך כל חוק בנפרד:

| סט | מה כלול | מתאים ל- |
|---|---|---|
| **מקל** | ספאם/הצפה קיצוני בלבד, התראה על שינוי הגדרות קבוצה | קבוצות חברים, קבוצות קטנות |
| **בינוני** (ברירת מחדל) | כל האמור לעיל + חסימת קישורים לא מוכרים, הודעות מועברות, חשיפת פרטי קשר | קבוצות קהילה, ועד בית, הורים |
| **מחמיר** | כל האמור לעיל + חלון זמן (שעות שקט), איסור סוגי מדיה, מילות מפתח ברירת מחדל, התראה על כל הצטרפות/עזיבה | קבוצות עסקיות, עובדים, מכירות |

סוגי החוקים הזמינים:
| סוג חוק | פרמטרים | דוגמה |
|---|---|---|
| מילות מפתח | רשימת מילים/ביטויים, Regex, התעלמות מרישיות/ניקוד | "הזמנות", "מחיר" |
| קישורים | חסימת כל הקישורים / רשימה לבנה / רשימה שחורה של דומיינים | אסור קישורי t.me |
| חלון זמן | שעות שבהן אסור לשלוח / יום בשבוע | אין הודעות 22:00–07:00; אין הודעות בשבת |
| ספאם/הצפה | N הודעות ב-X שניות מאותו משתמש | 5 הודעות ב-30 שניות |
| סוג מדיה | איסור שליחת סוג מדיה מסוים | אסור סטיקרים / אסור קול |
| הודעות מועברות | איסור Forwarded / Forwarded many times | |
| הצטרפות/עזיבה | התראה על עזיבת חבר / הצטרפות ממספר לא ישראלי | |
| שינוי הגדרות קבוצה | התראה על שינוי שם/תמונה/מנהלים | |
| מספרי טלפון / מיילים | זיהוי חשיפת פרטי קשר | |

#### 5.5.2 חוקים סמנטיים (מוגדרים בשפה חופשית)
- שפות נתמכות בסיווג: עברית ואנגלית בלבד בשלב זה.
- הלקוח כותב חוק בעברית/אנגלית, לדוגמה: *"אסור לפרסם מוצרים או שירותים בתשלום"*, *"אסור לדבר על פוליטיקה"*, *"אסור להעליב חברים אחרים גם בעקיפין או דרך סטיקרים"*.
- המערכת מציעה אוטומטית (בעזרת LLM) ניסוח מחודד + 3 דוגמאות חיוביות ו-3 שליליות, והלקוח מאשר/עורך.
- לכל חוק: רמת חומרה (נמוכה / בינונית / גבוהה / קריטית), סף ביטחון (ברירת מחדל 0.8), ופעולה.
- **Prompt לסיווג** כולל: כל חוקי הקבוצה, תיאור הקבוצה, ההודעה הנוכחית (כולל `ai_analysis` של מדיה), ו-N הודעות אחרונות. הפלט: JSON מובנה `{violations: [{rule_id, confidence, explanation, quoted_evidence}]}`.
- לחיסכון בעלויות: חוקים סמנטיים מופעלים רק אם ההודעה עברה סינון ראשוני (למשל: לא הודעה של "תודה" בת מילה אחת), ובחבילות Basic מוגבלים ל-3 חוקים.

#### 5.5.3 מנגנוני בקרה
| מזהה | דרישה |
|---|---|
| F-5.1 | **תבניות חוקים** מוכנות: קבוצת ועד בית, קבוצת הורים, קבוצת מכירות, קבוצת עובדים, קבוצת קהילה. |
| F-5.2 | **רשימה לבנה** – משתמשים (למשל מנהלי הקבוצה) שההודעות שלהם לא נבדקות או נבדקות בחומרה מופחתת. |
| F-5.3 | **Cooldown** – אותו חוק לא ייצור יותר מהתראה אחת לאותו משתמש ב-X דקות (ברירת מחדל 10), אלא אם החומרה קריטית. |
| F-5.4 | **מצב למידה (Shadow Mode)** – ב-48 השעות הראשונות ההפרות נרשמות בפאנל אך לא נשלחות התראות, כדי לאפשר ללקוח לכייל חוקים. |
| F-5.5 | **משוב** – על כל התראה בפאנל ניתן לסמן "נכון" / "שגוי". המשוב נשמר ומשמש כדוגמאות Few-Shot בפרומפט של אותה קבוצה (עד 20 דוגמאות אחרונות). בגרסה 2: משוב גם בתשובה ישירה להתראה בוואטסאפ, והעלאת דוגמאות להפרות שפוספסו (ראה סעיף 3.2). |
| F-5.6 | **בדיקת חוק** – מסך "נסה חוק": הלקוח מדביק הודעה לדוגמה ורואה איך המנוע היה מסווג אותה. |
| F-5.7 | זמן מקבלת ההודעה ועד החלטת המנוע: < 5 שניות לחוקים דטרמיניסטיים, < 15 שניות לחוקים סמנטיים (P95). |

### 5.6 התראות (Alerting)
| מזהה | דרישה |
|---|---|
| F-6.1 | לכל קבוצה: רשימת אנשי קשר להתראה (שם, מספר וואטסאפ, אופציונלית מייל). מגבלת כמות לפי חבילה. |
| F-6.2 | לכל איש קשר ניתן להגדיר: אילו רמות חומרה הוא מקבל, שעות שקט (Quiet Hours), ערוץ (וואטסאפ / מייל / שניהם). |
| F-6.3 | ההתראה נשלחת **מהמספר העסקי השולח** (מספר נפרד מהמספר הקורא, רשום מול Meta – ראה סעיף 10.2) **בהודעה פרטית** לאיש הקשר (לא לקבוצה), באמצעות תבנית הודעה מאושרת. איש הקשר צריך לאשר קבלת הודעות בפעם הראשונה (Opt-in) – המערכת שולחת הודעת "השב OK לאישור". |
| F-6.4 | **תבנית התראה** (ניתנת לעריכה): <br>`🚨 התראה מקבוצת "{group_name}"`<br>`חוק שהופר: {rule_name} ({severity})`<br>`מי: {sender_name} ({sender_phone})`<br>`מתי: {time}`<br>`תוכן: "{message_excerpt}"`<br>`הסבר: {ai_explanation}`<br>`לצפייה ולסימון: {short_link}` |
| F-6.5 | עבור מדיה – ההתראה כוללת את תיאור המדיה, ואופציונלית (הגדרה בקבוצה) את התמונה/הסטיקר עצמם. |
| F-6.6 | **קיבוץ** – אם 3+ הפרות מאותו משתמש בתוך דקה, נשלחת התראה מקובצת אחת. |
| F-6.7 | **אסקלציה** – אם התראה קריטית לא סומנה כ"טופלה" בפאנל תוך X דקות, נשלחת שוב לאיש קשר ברמה הבאה (אופציונלי, Pro ומעלה). |
| F-6.8 | כל התראה נרשמת ב-DB עם סטטוס: sent / delivered / read / failed, וניתנת לצפייה בפאנל. |
| F-6.9 | התראות מערכתיות ללקוח (לא הפרות): המספר נותק, המספר הוסר מהקבוצה, המספר נחסם, חריגה ממכסת ההודעות. |

### 5.7 דו"ח יומי במייל
| מזהה | דרישה |
|---|---|
| F-7.1 | נשלח פעם ביום, בריצה אחת מרוכזת בחצות שעון ישראל עבור כלל הקבוצות, לרשימת נמענים לכל קבוצה. דו"ח נפרד לכל קבוצה (ללא דו"ח מאוחד בשלב זה). |
| F-7.2 | **תוכן הדו"ח** (HTML responsive + גרסת טקסט): <br>• סיכום: סה"כ הודעות, שינוי מול אתמול ומול ממוצע 7 ימים <br>• פילוח לפי סוג: טקסט / תמונות / סטיקרים / קול / קישורים <br>• 10 המשתתפים הפעילים ביותר (שם, מס' הודעות, אחוז) <br>• מס' משתתפים ייחודיים שכתבו, מס' חברים בקבוצה, הצטרפויות ועזיבות <br>• התפלגות פעילות לפי שעה (גרף) <br>• **סיכום נושאים** – 3–5 הנושאים המרכזיים שנדונו, שנוצרו ע"י LLM (Pro ומעלה) <br>• **הפרות** – מספר ההתראות שנשלחו, פילוח לפי חוק וחומרה, רשימת ההפרות עם קישור לפאנל <br>• דגלי איכות: הודעות שנמחקו, הודעות שלא עובדו |
| F-7.3 | הדו"ח לא כולל תוכן מלא של הודעות (רק קטעים של הפרות), מטעמי פרטיות. |
| F-7.4 | הדו"ח נשלח תמיד, גם ביום ללא פעילות – היעדר פעילות עשוי להעיד על חריגה (למשל ניתוק המספר מהקבוצה) ולכן מסומן בדו"ח כדגל. אם אין פעילות בקבוצה מעל 14 ימים רצופים, הדו"ח יכלול המלצה ללקוח לנתק את הקבוצה מהמערכת (עם קישור לפעולה בפאנל). |
| F-7.5 | ארכיון דו"חות בפאנל (90 יום). |
| F-7.6 | משלוח דרך **Gmail API** (ראה סעיף 10.3). כתובת השולח: בשלב הראשוני – כתובת ה-Gmail הראשית שניתנה למערכת; כתובת ייעודית (reports@…) עם Reply-To לתמיכה תישקל בשלב מאוחר יותר. |

### 5.8 פאנל ניהול – לקוח (Client Portal)
מסכים:
1. **Dashboard** – סטטוס קבוצות, התראות של 24 שעות אחרונות, גרף פעילות, מכסת שימוש.
2. **קבוצות** – רשימה, הוספה, ניתוק, הגדרות (שם תצוגה, אזור זמן, שמירת מדיה, הודעת גילוי).
3. **חוקים** (לכל קבוצה) – רשימת חוקים, יצירה מטופס / משפה חופשית / מתבנית, הפעלה/כיבוי, סדר עדיפויות, "נסה חוק".
4. **אנשי קשר להתראות** – ניהול, סטטוס Opt-in, שעות שקט.
5. **התראות** – יומן (Log) עם סינון, צפייה בהודעה המקורית ובהקשר, סימון נכון/שגוי, סימון "טופל".
6. **דו"חות** – הגדרות נמענים ושעה, ארכיון.
7. **חברי קבוצה** – רשימה, סטטיסטיקות, הוספה לרשימה לבנה, שם תצוגה ידני (למקרה שאין שם בוואטסאפ).
8. **הגדרות חשבון** – משתמשים ותפקידים, מנוי וחיוב, מחיקת חשבון (Right to be forgotten).

### 5.9 פאנל ניהול – אדמין מערכת (Super-Admin)
1. רשימת Tenants – סטטוס, חבילה, שימוש, השעיה/הפעלה, כניסה למצב Impersonate (עם רישום Audit).
2. **מאגר מספרים וירטואליים** – רשימת מספרים, ספק, סטטוס (פנוי / בשימוש / חסום / בחימום), Tenant משויך, כמות הודעות יומית. **התראה קריטית ייעודית** כשמספר מתקרב לסף שעלול לגרום לחסימה (קצב שליחה, כמות נמענים חדשים ביום, סימני חסימה מהספק) – נשלחת מיידית לאדמין המערכת בוואטסאפ ובמייל, ומופיעה כבאנר בולט בפאנל. זוהי ההתראה התפעולית החשובה ביותר במערכת, ובשלב הראשוני היא ההתראה התפעולית היחידה שחובה לממש.
3. **בריאות מערכת** – עומק תורים, זמני עיבוד, שיעור כשלים במודלים, עלויות AI לפי Tenant.
4. ניהול תבניות חוקים גלובליות.
5. יומן Audit של פעולות אדמין.
6. Feature Flags.

---

## 6. מודל נתונים (ישויות עיקריות)

```
tenants            (id, name, plan, status, timezone, locale, created_at)
users              (id, tenant_id, email, password_hash, role, last_login)
subscriptions      (id, tenant_id, plan, billing_provider_id, period_end, limits JSONB)

virtual_numbers    (id, phone_e164, role ENUM(reader|sender), provider, provider_session_id, status,
                    assigned_tenant_id, health JSONB, created_at)

groups             (id, tenant_id, virtual_number_id, provider_group_id,
                    name, description, status, settings JSONB, connected_at)
group_members      (id, group_id, phone_hash, phone_e164_enc, display_name,
                    is_admin, whitelisted, joined_at, left_at)

messages           (id, group_id, provider_message_id UNIQUE, member_id,
                    type ENUM, text, media_id, reply_to_id, is_forwarded,
                    is_deleted, is_edited, original_text, sent_at, received_at,
                    ai_analysis JSONB, processing_status)
media              (id, tenant_id, storage_key, mime, size, sha256,
                    analysis JSONB, expires_at)
sticker_cache      (sha256 PK, description, sentiment, flags JSONB)

rules              (id, group_id, type ENUM(deterministic|semantic), name,
                    config JSONB, severity, confidence_threshold, enabled,
                    cooldown_minutes, order)
rule_examples      (id, rule_id, text, is_violation, source ENUM(user|feedback))

violations         (id, group_id, message_id, rule_id, confidence,
                    explanation, status ENUM(open|handled|false_positive),
                    feedback, created_at)

alert_contacts     (id, group_id, name, phone_e164, email, channels[],
                    min_severity, quiet_hours JSONB, optin_status, escalation_level)
alerts             (id, violation_id, contact_id, channel, status,
                    provider_msg_id, sent_at, delivered_at, read_at, error)

reports            (id, group_id NULL, tenant_id, date, payload JSONB,
                    html_storage_key, sent_to[], sent_at, status)
daily_stats        (group_id, date, member_id NULL, msg_count, by_type JSONB)

audit_log          (id, tenant_id, actor_user_id, action, target, meta JSONB, at)
```

הערות:
- מספרי טלפון של חברי קבוצה נשמרים מוצפנים (AES-256, מפתח לכל Tenant) + hash לחיפוש.
- `ai_analysis` ו-`config` כ-JSONB כדי לאפשר גמישות ללא מיגרציות תכופות.
- אינדקסים: `messages(group_id, sent_at)`, `violations(group_id, created_at)`, `alerts(status)`.

---

## 7. תהליכים מרכזיים (Flows)

### 7.1 עיבוד הודעה נכנסת
1. `WA Connector` מקבל Webhook מהספק → מאמת חתימה → מנרמל ל-`InboundEvent` אחיד → דוחף לתור `inbound`.
2. `Ingestion Worker`: בודק Idempotency, מזהה Tenant/Group, שומר `messages` (status=`received`). אם מדיה – מוריד, שומר ב-Storage, מחשב hash.
3. אם הודעת מדיה → תור `media_analysis`. `Media Worker` בודק cache, אחרת קורא למודל Vision, מעדכן `ai_analysis`.
4. תור `rules`: `Rules Engine` מריץ חוקים דטרמיניסטיים (מיידי). אם יש חוקים סמנטיים פעילים ועברה הסינון הראשוני – קריאת LLM עם הקשר.
5. לכל הפרה: בדיקת whitelist, cooldown, shadow mode → יצירת `violations` → תור `alerts`.
6. `Notification Service`: לכל contact רלוונטי – בדיקת quiet hours, opt-in, קיבוץ → שליחה דרך `WhatsAppProvider.send_direct()` → עדכון סטטוס.
7. `Analytics Worker` מעדכן `daily_stats` (upsert).

### 7.2 דו"ח יומי
1. `Scheduler` (Celery Beat) מריץ פעם ביום בחצות (00:00 שעון ישראל, Asia/Jerusalem) ומייצר את הדו"ח עבור כלל הקבוצות הפעילות במערכת, על נתוני היום שהסתיים.
2. `Report Builder` שולף `daily_stats` + `violations`, מייצר סיכום נושאים (LLM), מרנדר HTML (Jinja2) ומייצר גרפים (matplotlib → PNG מוטמע / SVG).
3. שליחה דרך `Gmail API` (Retry עם backoff). שמירת עותק ב-Storage ורשומת `reports`.

### 7.3 Onboarding קבוצה
הרשמה → בחירת חבילה → "הוסף קבוצה" → הקצאת מספר → המתנה לאירוע `group_join` (timeout 24 שעות) → אישור הלקוח → הודעת גילוי → בחירת תבנית חוקים → הוספת איש קשר ראשון (opt-in) → Shadow Mode 48 שעות → פעיל.

---

## 8. דרישות לא-פונקציונליות

| תחום | דרישה |
|---|---|
| **ביצועים** | 1,000 הודעות/שנייה בשיא במערכת כולה; קבוצה בודדת עד 50 הודעות/שנייה. |
| **זמינות** | 99.5% לשירות הקליטה; חלון תחזוקה מתוכנן עד 2 שעות/חודש. |
| **סקלביליות** | Workers ניתנים להרחבה אופקית; אין state בתהליכי העיבוד. |
| **אבטחה** | TLS בכל התעבורה; הצפנה At-Rest; סיסמאות ב-Argon2; 2FA לאדמין; Rate limiting; OWASP Top-10; הפרדת Tenants ב-RLS; Secrets ב-Vault/Secret Manager ולא בקוד. |
| **פרטיות** | עמידה בחוק הגנת הפרטיות (ישראל) ו-GDPR: הודעת גילוי בקבוצה, מדיניות שמירה (ברירת מחדל: הודעות 45 יום, מדיה 30 יום), מחיקה לפי בקשה, ייצוא נתונים, DPA ללקוחות. **המערכת לא משתמשת בתוכן הלקוחות לאימון מודלים**. |
| **ניטור** | Logs מובנים (JSON), Metrics (Prometheus), Tracing (OpenTelemetry), התראות תפעוליות (PagerDuty/Slack). |
| **עלויות AI** | מדידת טוקנים לכל Tenant; תקרת עלות יומית לכל Tenant עם Degradation (מעבר לחוקים דטרמיניסטיים בלבד) במקום עצירה. |
| **גיבויים** | גיבוי DB יומי, שמירה 30 יום, בדיקת שחזור רבעונית. |
| **נגישות ו-RTL** | פאנל בעברית מלאה RTL, תמיכה בדפדפנים מודרניים, Responsive למובייל. |
| **עיצוב ומיתוג** | עיצוב גנרי אחיד לכל הלקוחות, בסגנון צבעים שמזכיר את וואטסאפ (ירוק) עם נגיעה של צבע נוסף אחד. ללא מיתוג לבן (White-label) וללא התאמת מיתוג ללקוח בשלב זה. |

---

## 9. סטאק טכנולוגי

| רכיב | בחירה | הערה |
|---|---|---|
| שפה | **Python 3.12+** | כנדרש |
| Web / API | **FastAPI** + Pydantic v2 | REST פנימי + Webhooks |
| פאנל HTML | **Jinja2 templates + HTMX + Tailwind CSS** (או Alpine.js) | דרישה ל-HTML פשוט; ללא SPA כבד. אופציה: Django Admin לפאנל הסופר-אדמין. |
| ORM / DB | **SQLAlchemy 2 + Alembic**, **PostgreSQL 16** | RLS, JSONB |
| תורים | **Celery + Redis** (או Redis Streams + arq) | Celery Beat לתזמון |
| Object Storage | S3-compatible (AWS S3 / Cloudflare R2 / MinIO) | מדיה, דו"חות |
| AI – טקסט | Claude (API של Anthropic) – מודל Haiku לסיווג, Sonnet לסיכומים | Structured Output (JSON) |
| AI – ראייה | אותו API עם קלט תמונה | סטיקרים מומרים מ-WebP ל-PNG לפני שליחה |
| מיילים | **Gmail API** (google-api-python-client) | סעיף 10.3 |
| וואטסאפ | שכבת `WhatsAppProvider` + ספק לפי החלטה בסעיף 10.2 | |
| אימות | fastapi-users / Authlib (Google OAuth) | |
| חיוב | ספק סליקה ישראלי (יוחלט בשלב ה-SaaS) | לא נדרש בגרסאות הראשוניות |
| Deployment | Docker, docker-compose לפיתוח; Kubernetes או Fly.io/Railway לפרודקשן | |
| CI/CD | GitHub Actions: lint (ruff), types (mypy), tests (pytest, כיסוי ≥ 70%) | |

---

## 10. אינטגרציות חיצוניות – פירוט

### 10.1 ממשק `WhatsAppProvider` (חובה לממש כ-Interface)
```python
class WhatsAppProvider(Protocol):
    async def create_session(self, number: VirtualNumber) -> SessionInfo
    async def get_session_status(self, session_id) -> SessionStatus
    async def list_groups(self, session_id) -> list[GroupInfo]
    async def get_group_members(self, session_id, group_id) -> list[Member]
    async def download_media(self, session_id, media_ref) -> bytes
    async def send_direct_message(self, session_id, to_e164, text, media=None) -> SendResult
    async def send_group_message(self, session_id, group_id, text) -> SendResult
    def parse_webhook(self, headers, body) -> list[InboundEvent]   # + אימות חתימה
```

### 10.2 ספק וואטסאפ ומספר וירטואלי – ⚠️ סיכון מרכזי, נדרשת החלטה לפני פיתוח
| אפשרות | יתרונות | חסרונות |
|---|---|---|
| **A. WhatsApp Business Platform (Cloud API רשמי של Meta)** | לגיטימי, יציב, ללא סיכון חסימה | **תמיכה בקבוצות מוגבלת/חלקית** – יש לוודא מול התיעוד העדכני של Meta האם קריאת הודעות בקבוצות זמינה לחשבון שלנו. הודעות פרטיות יזומות (התראות) דורשות תבניות מאושרות ותשלום לשיחה. |
| **B. ספק מבוסס פרוטוקול Multi-Device** (למשל WAHA, Green-API, Evolution API, או Baileys עטוף בשירות) | תמיכה מלאה בקבוצות, סטיקרים, מדיה, שליחה חופשית | **מנוגד לתנאי השימוש של וואטסאפ**; סיכון לחסימת מספרים; דורש "חימום" מספרים ופול חלופי; סיכון עסקי שיש לשקף ללקוחות. |
| **C. היברידי** | קליטה דרך B, שליחת התראות דרך A | מורכבות כפולה |

**החלטה: אפשרות C – היברידי, בהפרדה לשני מספרים:**
- **מספר קורא (Reader)** – מספר וירטואלי שמצורף לקבוצה וקורא את ההודעות בלבד (אפשרות B). לא שולח הודעות כלל, למעט הודעת הגילוי בעת ההצטרפות. הפרדה זו מקטינה משמעותית את הסיכון לחסימה, כי המספר אינו מבצע שליחות יזומות.
- **מספר שולח (Sender)** – מספר עסקי נפרד, רשום ומאומת מול Meta כ-WhatsApp Business (Cloud API, אפשרות A). כל ההתראות לאנשי הקשר וההודעות המערכתיות יוצאות ממנו, באמצעות תבניות הודעה מאושרות (Message Templates) והסכמת נמען (Opt-in).
- ה-`WhatsAppProvider` Interface ימומש פעמיים: `ReaderProvider` (קליטה בלבד) ו-`SenderProvider` (שליחה בלבד).
- מנגנוני הגנה על המספר הקורא: Pool של מספרים, ניטור בריאות, והתראה קריטית על סימני חסימה (סעיף 5.9).

**מספרים וירטואליים:** רכישה מספק SIM/VoIP – מספרים בינלאומיים (לא ישראליים) לצורך הוזלת עלויות, רישום המספר בוואטסאפ (סריקת QR / קוד SMS – פעולה ידנית של אדמין ב-MVP, אוטומטית בשלב 2), חימום 3–5 ימים לפני הקצאה ללקוח.

### 10.3 Gmail API
- **אופן החיבור (שלב ראשוני):** OAuth2 Refresh Token לחשבון ה-Gmail הראשי שניתן למערכת, scope: `https://www.googleapis.com/auth/gmail.send`. בשלב מאוחר יותר: Google Workspace של הפלטפורמה + **Service Account עם Domain-Wide Delegation** למשתמש ייעודי (`reports@<domain>`).
- מבנה מייל: MIME multipart (text/plain + text/html), תמונות מוטמעות כ-CID או SVG inline, Subject: `דו"ח יומי – {group_name} – {date}`.
- מכסות: Gmail API מגביל שליחה (~2,000 מיילים/יום למשתמש Workspace). לתכנן **מספר חשבונות שולחים** ו-Round-Robin, או מעבר ל-Google Workspace Email API/SMTP Relay בסקייל. יש לוודא את המכסה העדכנית מול Google.
- אימות דומיין: SPF, DKIM, DMARC כדי למנוע נפילה לספאם.
- טיפול ב-Bounce: קריאת תיבת החשבון השולח (scope `gmail.readonly`) לזיהוי מיילים חוזרים וסימון כתובות לא תקינות בפאנל.

### 10.4 Anthropic API (מודלי שפה וראייה)
- Prompt Caching על החלק הקבוע של הפרומפט (חוקי הקבוצה + דוגמאות) – מפחית עלויות משמעותית.
- Structured Output / JSON Schema לפלט הסיווג.
- Timeout 20 שניות, Retry ×2, Fallback: סימון `processing_status=ai_failed` והרצה חוזרת בתור איטי.
- אין שליחת מספרי טלפון למודל – רק שמות תצוגה / מזהים אנונימיים.

---

## 11. API פנימי (תמצית)
```
POST   /api/v1/webhooks/whatsapp/{provider}      # קליטת אירועים (חתום)
GET    /api/v1/groups                            # רשימת קבוצות ל-Tenant
POST   /api/v1/groups                            # התחלת Onboarding
GET/PUT/DELETE /api/v1/groups/{id}
GET/POST /api/v1/groups/{id}/rules
PUT/DELETE /api/v1/rules/{id}
POST   /api/v1/rules/test                        # "נסה חוק"
GET/POST /api/v1/groups/{id}/contacts
GET    /api/v1/violations?group_id=&status=&from=&to=
PATCH  /api/v1/violations/{id}                   # feedback / handled
GET    /api/v1/reports?group_id=
POST   /api/v1/reports/{group_id}/send-now       # שליחה ידנית
GET    /api/v1/stats/{group_id}?range=
--- admin ---
GET/POST /admin/api/tenants, /admin/api/numbers, /admin/api/health
```
כל בקשה נושאת JWT עם `tenant_id` ו-`role`; אדמין-על עם claim נפרד.

---

## 12. תוכנית פיתוח מוצעת

| שלב | תכולה |
|---|---|
| **0 – POC** | לפי סעיף 3.0: אימות ספק וואטסאפ (קבוצות, סטיקרים, שליחה פרטית), אימות Gmail API, סיווג עם Claude על קבוצה אמיתית אחת. ללא חיבור לספק תשלומים |
| **1 – תשתית** | Tenants, Auth, DB, תורים, WA Connector + Interface, Ingestion, Storage |
| **2 – מוח** | Media Understanding, Rules Engine (דטרמיניסטי + סמנטי), Violations, Feedback |
| **3 – ערוצי יציאה** | Notification Service (וואטסאפ + opt-in), Daily Report + Gmail |
| **4 – פאנל** | Client Portal + Super-Admin (HTML) |
| **5 – SaaS** | חיוב, חבילות ומגבלות, Onboarding מלא, מדיניות שמירה ומחיקה |
| **6 – הקשחה** | אבטחה, ניטור, בדיקות עומס, Beta עם 5 לקוחות |

---

## 13. קריטריוני קבלה (Acceptance Criteria) – דוגמאות
- הודעת טקסט הכוללת קישור t.me בקבוצה עם חוק "אסור קישורי טלגרם" → התראה מגיעה לאיש הקשר תוך 10 שניות, ומופיעה ביומן עם ההודעה המקורית.
- סטיקר לועג שנשלח כתגובה להודעת אבל בקבוצה עם חוק "אסור לעג" → זוהה כהפרה עם ביטחון ≥ 0.8; אותו סטיקר בהקשר של בדיחה → לא זוהה.
- אותו `provider_message_id` נשלח פעמיים ב-Webhook → רשומה אחת ב-DB, התראה אחת.
- דו"ח יומי מגיע לאחר ריצת חצות (שעון ישראל), עם נתונים התואמים ל-DB, ולא נופל לספאם ב-Gmail/Outlook.
- משתמש ב-Tenant A לא יכול לגשת ל-URL של קבוצה של Tenant B (403).
- מחיקת חשבון מוחקת את כל הנתונים, כולל מדיה ב-Storage, תוך 24 שעות.

---

## 14. שאלות פתוחות
כל השאלות הפתוחות נסגרו וההחלטות שוקעו בגוף המסמך (ראה בעיקר סעיפים 3, 8, 10.2). סוגיות משפטיות של תנאי השימוש במוצר (לרבות ניסוח הסיכון בנוגע למספר הקורא) אינן נדרשות בשלב הראשוני כל עוד המערכת אינה מסחרית, ויטופלו לקראת המעבר לשלב ה-SaaS.
